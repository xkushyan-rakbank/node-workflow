import { makeStyles } from "@material-ui/core/styles";

export const styles = makeStyles({
  checkboxesWrapper: {
    "&>label": {
      marginBottom: "20px"
    }
  },
  listItem: {
    display: "block"
  }
});
