import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  centeredContainer: {
    marginTop: 0
  },
  heading: {
    fontSize: 48,
    lineHeight: "56px"
  },
  title: {
    marginTop: 0,
    fontSize: 20
  }
});
