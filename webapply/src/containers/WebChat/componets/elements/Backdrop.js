import styled from "styled-components";

export const Backdrop = styled.div`
  position: fixed;
  z-index: 8;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  margin: 0 !important;
`;
