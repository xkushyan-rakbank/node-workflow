import { styled } from "@material-ui/styles";
import { ContainedButton } from "../../../components/Buttons/ContainedButton";

export const GoToHomePageButton = styled(ContainedButton)({
  fontSize: "18px",
  width: "220px",
  padding: "8px 18px"
});
