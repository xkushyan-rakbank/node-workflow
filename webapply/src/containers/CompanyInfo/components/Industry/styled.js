import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  industrySubCategory: {
    fontSize: "10px"
  },
  continueButtonGrid: {
    padding: 20
  }
}));
