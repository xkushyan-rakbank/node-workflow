import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  inputSkeleton: {
    width: "100%",
    height: "56px",
    borderRadius: "8px",
    marginBottom: "24px"
  }
});
